export const metadata = {
  title: "Malaysia Gateway",
  description: "Malaysia work visa agency",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}